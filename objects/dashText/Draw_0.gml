draw_set_font(GameFont);

draw_text(x, y, "While in the air");
draw_text(x, y + 100, "Press the jump button again to");
draw_text(x, y + 200, "double jump or press");
draw_text(x, y + 300, "shift to dash.");
